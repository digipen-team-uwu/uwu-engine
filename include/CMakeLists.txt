#Populate headers
file(GLOB_RECURSE ENGINE_CORE_HEADER UWUEngine/*.h)
file(GLOB_RECURSE ENGINE_CORE_HPP_HEADER UWUEngine/*.hpp)

set(ENGINE_CORE_HEADERS
        ${ENGINE_CORE_HEADER}
        ${ENGINE_CORE_HPP_HEADER}

        PARENT_SCOPE
)

file(GLOB_RECURSE TESTS_HEADER UWUTest/*.h)
file(GLOB_RECURSE TESTS_HPP_HEADER UWUTest/*.hpp)

set(TESTS_HEADERS
        ${TESTS_HEADER}
        ${TESTS_HPP_HEADER}

        PARENT_SCOPE
)
